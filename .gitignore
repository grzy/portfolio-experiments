# deps
node_modules/
**/node_modules/

# builds
build/
dist/
**/build/
**/dist/

# env
.env
.env.local
.env.*.local

# landing-page is the old Frankenstein React site (Jan 2026, built w/ Haiku).
# It lives as its own git repo pointing to github.com/grzy/badges.
# Kept locally for reference while we rebuild -- do NOT delete.
landing-page/

# macOS
.DS_Store
**/.DS_Store

# editor
.vscode/
.idea/
*.swp

# firebase deploy cache
.firebase/

# huge source images -- kept local for re-cropping, not committed
experiments/hero-nav-neon-01/images/tui-product-shot1.png
experiments/hero-nav-neon-01/images/lost-model6.png

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
